const json = {
  prettify: `Takes two arguments: value, indent
             Converts a value to JSON string.
             When the passed value is not a string, returns the value.`
}
module.exports = json
